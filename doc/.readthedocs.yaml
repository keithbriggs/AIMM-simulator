version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

sphinx:
  configuration: doc/sphinx_source/conf.py

# Explicitly set the version of Python and its requirements
python:
  install:
    - requirements: doc/requirements.txt
